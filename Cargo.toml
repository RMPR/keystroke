[package]
name = "gui_typing_tutor_demo"
version = "0.1.0"
edition = "2021"

[dependencies]
iced = { version = "0.12", features = ["tokio"] } # Or "async-std", check Iced docs for options
once_cell = "1"
# std::time::Instant is part of standard library
